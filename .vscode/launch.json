{
  "version": "0.2.0",
  "configurations": [{
    "name": "BeProgram Extension",
    "type": "extensionHost",
    "request": "launch",
    "runtimeExecutable": "${execPath}",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}/apps/vscode-extension"],
    "preLaunchTask": "Build BeProgram sidebar"
  }]
}
